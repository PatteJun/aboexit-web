---
import Layout from '../../layouts/Layout.astro';
import CancellationLetter from '../../components/CancellationLetter.astro';
import { providers } from '../../data/providers';

export function getStaticPaths() {
  return providers.map((provider) => ({
    params: { provider: provider.slug },
    props: { provider },
  }));
}

const { provider } = Astro.props;
---

<Layout 
  title={provider.metaTitle}
  description={provider.metaDescription}
>
  <div class="container">
    <article class="content">
      <nav class="breadcrumb">
        <a href="/">Startseite</a>
        <span>/</span>
        <span>Kündigung {provider.name}</span>
      </nav>

      <h1>Kündigung {provider.name}</h1>
      
      <div class="info-box">
        <h2>Wichtige Informationen zur Kündigung</h2>
        <ul>
          <li>Kündigungsfrist beachten</li>
          <li>Schriftliche Form einhalten</li>
          <li>Kundennummer bereithalten</li>
          <li>Bestätigung aufbewahren</li>
        </ul>
      </div>

      <section class="letter-section">
        <h2>Ihre Kündigungsvorlage</h2>
        <CancellationLetter provider={provider} />
      </section>

      <section class="instructions">
        <h2>So gehen Sie vor</h2>
        <div class="steps">
          <div class="step">
            <div class="step-number">1</div>
            <div class="step-content">
              <h3>Vorlage herunterladen und anpassen</h3>
              <p>Fügen Sie Ihre persönlichen Daten in die markierten Stellen ein.</p>
            </div>
          </div>
          <div class="step">
            <div class="step-number">2</div>
            <div class="step-content">
              <h3>Unterlagen vorbereiten</h3>
              <p>Legen Sie alle notwendigen Dokumente wie Personalausweis-Kopie bereit.</p>
            </div>
          </div>
          <div class="step">
            <div class="step-number">3</div>
            <div class="step-content">
              <h3>Kündigung versenden</h3>
              <p>Senden Sie die Kündigung per Einschreiben an die angegebene Adresse.</p>
            </div>
          </div>
        </div>
      </section>

      <section class="faq">
        <h2>Häufige Fragen zur Kündigung</h2>
        <div class="questions">
          <details>
            <summary>Welche Kündigungsfrist muss ich beachten?</summary>
            <p>Die Kündigungsfrist hängt von Ihrem individuellen Vertrag ab. Standardmäßig beträgt sie meist 3 Monate zum Vertragsende.</p>
          </details>
          <details>
            <summary>Muss ich einen Grund angeben?</summary>
            <p>Nein, bei einer ordentlichen Kündigung zum Vertragsende müssen Sie keinen Grund angeben.</p>
          </details>
          <details>
            <summary>Wie erhalte ich eine Bestätigung?</summary>
            <p>Der Anbieter muss Ihnen den Erhalt der Kündigung und das Kündigungsdatum schriftlich bestätigen.</p>
          </details>
        </div>
      </section>
    </article>
  </div>
</Layout>

<style>
  .content {
    max-width: 800px;
    margin: 0 auto;
  }

  .breadcrumb {
    margin-bottom: 2rem;
    color: var(--text-light);
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .breadcrumb a {
    color: var(--primary-color);
    text-decoration: none;
  }

  .breadcrumb a:hover {
    color: var(--primary-light);
  }

  h1 {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--text-color);
    margin-bottom: 2rem;
  }

  .info-box {
    background: #f0fdfa;
    border: 1px solid #99f6e4;
    border-radius: 1rem;
    padding: 2rem;
    margin-bottom: 3rem;
  }

  .info-box h2 {
    color: var(--primary-color);
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }

  .info-box ul {
    list-style-type: none;
  }

  .info-box li {
    margin-bottom: 0.75rem;
    padding-left: 1.75rem;
    position: relative;
  }

  .info-box li::before {
    content: "✓";
    position: absolute;
    left: 0;
    color: var(--primary-color);
    font-weight: bold;
  }

  .letter-section {
    margin: 3rem 0;
  }

  .letter-section h2 {
    font-size: 1.5rem;
    margin-bottom: 2rem;
  }

  .instructions {
    margin: 4rem 0;
  }

  .instructions h2 {
    font-size: 1.5rem;
    margin-bottom: 2rem;
  }

  .steps {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .step {
    display: flex;
    gap: 1.5rem;
  }

  .step-number {
    background: var(--primary-color);
    color: var(--white);
    width: 2.5rem;
    height: 2.5rem;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    flex-shrink: 0;
  }

  .step-content h3 {
    font-size: 1.25rem;
    margin-bottom: 0.5rem;
  }

  .faq {
    margin: 4rem 0;
  }

  .faq h2 {
    font-size: 1.5rem;
    margin-bottom: 2rem;
  }

  .questions details {
    border: 1px solid var(--border-color);
    border-radius: 0.5rem;
    padding: 1.25rem;
    margin-bottom: 1rem;
  }

  .questions summary {
    font-weight: 500;
    cursor: pointer;
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .questions summary::after {
    content: "+";
    color: var(--primary-color);
    font-weight: bold;
    font-size: 1.25rem;
  }

  .questions details[open] summary::after {
    content: "−";
  }

  .questions p {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px solid var(--border-color);
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2rem;
    }

    .info-box {
      padding: 1.5rem;
    }
  }
</style>